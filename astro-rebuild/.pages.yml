media:
  input: public/images
  output: /images

content:
  - name: writing
    label: Writing
    type: collection
    path: src/content/writing
    filename: "{primary}.md"
    view:
      fields: [title, publication, publicationDate, language, type, draft]
      primary: title
      sort: [publicationDate, title]
      default:
        sort: publicationDate
        order: desc
    fields:
      - name: title
        label: Title
        type: string
        required: true
      - name: slug
        label: Website slug
        type: string
        required: true
        pattern: "^[a-z0-9-]+$"
      - name: publication
        label: Publication
        type: string
        required: true
      - name: publicationDate
        label: Publication date
        type: string
        required: true
        description: "Use YYYY, YYYY-MM or YYYY-MM-DD. Do not invent missing day/month precision."
        pattern:
          regex: "^\\d{4}(-\\d{2}(-\\d{2})?)?$"
          message: "Use YYYY, YYYY-MM or YYYY-MM-DD."
      - name: displayDate
        label: Display date
        type: string
        description: "Optional human-readable form such as April/May 2010."
      - name: language
        label: Language
        type: select
        required: true
        options:
          values: [English, Italian]
      - name: type
        label: Article type
        type: select
        required: true
        options:
          values: [Review, Interview, Feature, Live report, Politics / feature, Blog, Commissioned feature, Other]
      - name: description
        label: Description / subject
        type: text
      - name: byline
        label: Byline
        type: string
        description: "Use when the original piece has a credited co-author or non-standard byline."
      - name: originalUrl
        label: Original URL
        type: string
      - name: legacyPath
        label: Legacy website path
        type: string
      - name: magazineIssue
        label: Magazine issue
        type: string
      - name: printedPages
        label: Printed page(s)
        type: string
      - name: cover
        label: Magazine cover
        type: image
      - name: featured
        label: Featured on homepage
        type: boolean
      - name: draft
        label: Draft
        type: boolean
      - name: body
        label: Article body
        type: rich-text
        options:
          format: markdown
          media: false
