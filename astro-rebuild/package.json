{
  "name": "alexfranquelli-com-astro",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/check-redirects.mjs && pagefind --site dist",
    "preview": "astro preview",
    "preview:cloudflare": "npm run build && wrangler preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "astro": "^7.3.0"
  },
  "devDependencies": {
    "pagefind": "^1.4.0",
    "wrangler": "^4.138.0"
  }
}
